# Memory Log - 2026-03-07

## 2026-03-07 11:22 UTC — Heartbeat Check
- **Email Check:** Unable to check (send-email.py tool requires --to/--subject/--body flags for sending; no inbox-check mode)
- **Projects Check:** ~/codex clean (no uncommitted changes); ~/go not a git repository
- **Memory Maintenance:** Distilling 2026-03-06 infra work into MEMORY.md
- **Notable:**
  - Disk pressure hardening: Added severity tiers (ALERT >80%, CRITICAL >90%), inode pressure signal, top-usage triage in `tools/infra-status.py`
  - Backup permission hardening: Added `--enforce-permissions` flag to `scripts/verify-backup-integrity.sh` and strict mode + umask 077 to `scripts/nightly-backup.sh`
  - Security-audit quality: Removed `rg` dependency from `tools/infra-autopilot.py` security-audit task
- **Status:** All systems nominal (email check unavailable, projects clean, memory distillation in progress)

## 2026-03-07 11:38 UTC — Night infra sprint hardening
- Selected top autonomous infra task from TODO + latest infra logs by upgrading `tools/infra-autopilot.py` from round-robin to risk-based task selection using the newest per-task artifacts plus the newest `infra-status` artifact.
- Hardened `tools/infra-status.py` and `tools/infra-autopilot.py` security-audit output:
  - suppress duplicate/noisy socket listings with sorted `ss` output
  - add explicit firewall visibility section (`ufw`/`nft`/`iptables` presence)
  - add actionable guidance for unexpected `udp/5353` mDNS exposure on public/cloud hosts
- Verification:
  - `python3 -m py_compile tools/infra-autopilot.py tools/infra-status.py` passed
  - `python3 tools/infra-status.py` generated `departments/infra/artifacts/checks/20260307T113639Z-infra-status.md`
  - `python3 tools/department-commands.py run infra` now selects `Monitor disk usage` because latest infra-status reports root `/` at `92%`
  - new infra artifacts confirmed at mode `600`
- Live risks surfaced:
  - root filesystem `/` is `92%` used (`17G/19G`, `1.6G` free)
  - unexpected external `udp/5353` listener remains present
  - `ufw` unavailable and no host firewall tool detected from current host checks
  - `1` package update pending

## 2026-03-07 12:12 UTC — OpenClaw proactive config item applied
- Executed the first setup action item by applying a corrected low-risk config plan to the live runtime after discovering the originally staged heartbeat keys were stale for OpenClaw `2026.2.24`.
- Schema discovery from the installed package showed heartbeat supports `every`, `activeHours.{start,end,timezone}`, `model`, `session`, `includeReasoning`, `target`, `to`, `accountId`, `prompt`, `ackMaxChars`, and `suppressToolErrorWarnings`; rejected keys were `intervalMs`, `lightContext`, and `activeHours.enabled`.
- Applied runtime changes:
  - `agents.defaults.heartbeat.target = "last"`
  - `agents.defaults.heartbeat.every = "30m"`
  - `agents.defaults.heartbeat.activeHours = {start: "08:00", end: "23:00", timezone: "Australia/Sydney"}`
  - proactive heartbeat prompt updated to send one actionable suggestion or `HEARTBEAT_OK`
  - `cron.jobs["Coding Status Push"].delivery.mode = "announce"`
  - coding/finance status-push prompts updated to print script stdout exactly and avoid `NO_REPLY`
- Verification:
  - runtime `~/.openclaw/openclaw.json` and `~/.openclaw/cron/jobs.json` reflect the new values
  - `systemctl --user is-active openclaw-gateway` returned `active`
  - `openclaw health` showed heartbeat interval `30m (main)`
- Remaining caveat:
  - `openclaw system heartbeat last` returned `null` immediately after restart because no new cycle had fired yet
  - manual `openclaw cron run` validation for `Coding Status Push` hit `Unhandled stop reason: network_error` in gateway logs and surfaced as gateway close `1006`, so end-to-end delivery still needs confirmation on the next successful scheduled job run

## 2026-03-07 12:29 UTC — Retention + status-push fixes verified
- `~/.openclaw/openclaw.json` is currently mode `600`; `openclaw security audit --deep` no longer reports config-file permissions and now only flags small-model fallback/tooling risk plus two warnings.
- Repaired `tools/session-retention.sh`:
  - replaced the broken quoted JSON-array construction with per-action `jq` object output
  - added `SESSIONS_DIR` override so the script can be tested safely outside the live store
  - validated with `bash -n` and a temp-directory run that removed only the stale test file
  - executed the live cleanup once and removed `deef22f6-d1a3-419d-80ec-5eaed45d2361.jsonl.deleted.2026-03-06T22-03-42.860Z` (38h old, 17680 bytes)
- Status-push verification:
  - `Finance Status Push` debug run succeeded on NVIDIA and now records `deliveryStatus: delivered`
  - `Coding Status Push` prompt/delivery fix works, but the old `zai/glm-4.7-Flash` model kept returning `Unhandled stop reason: network_error`
  - switched `Coding Status Push.payload.model` to `nvidia/qwen/qwen3.5-397b-a17b`
  - post-change coding debug run succeeded with `status: ok`, provider `nvidia`, and `deliveryStatus: delivered`
- `openclaw doctor` still reports broader session-store drift:
  - legacy keys in `~/.openclaw/agents/main/sessions/sessions.json`
  - 2/5 recent sessions missing transcripts
  - 3 orphan transcript files

## 2026-03-07 12:34 UTC — Session-store drift cleaned
- Ran `openclaw doctor --fix --non-interactive --yes`; it canonicalized 1 remaining legacy session key in `~/.openclaw/agents/main/sessions/sessions.json`.
- Manual cleanup then removed 4 dead main-store entries that pointed at missing transcripts:
  - `agent:main:telegram:group:@jknopf`
  - `agent:main:telegram:slash:156480904`
  - `agent:main:cron:16457126-51c8-4708-aa4a-3140c590ef1f:run:a6daf9ac-ca01-40a2-b94d-88fc8fbc0cb5`
  - `agent:main:telegram:group:-1002381931352`
- Tombstoned 3 orphan transcript files by renaming them to `.jsonl.deleted.<timestamp>` and kept a full backup of the prior store at `/tmp/sessions.json.backup-2026-03-07T12-33-37.365287Z`.
- Reran `tools/session-retention.sh`; it immediately purged the two tombstoned transcripts older than 24h:
  - `74883967-9b7b-4871-b8d8-ca397fe9f77f.jsonl.deleted.2026-03-07T12-33-37.365287Z` (`81h`, `921974` bytes)
  - `2e9b0345-58ee-4cd3-ae1b-2f89c1c5f92b.jsonl.deleted.2026-03-07T12-33-37.365287Z` (`32h`, `679535` bytes)
- Current deleted transcript set left for future retention:
  - `a6daf9ac-ca01-40a2-b94d-88fc8fbc0cb5.jsonl.deleted.2026-03-06T13-25-33.729Z` (not yet 24h at the time of cleanup)
  - `f90d3ac2-3907-4740-9c19-6c253ec53ba1.jsonl.deleted.2026-03-07T12-33-37.365287Z` (fresh tombstone)
- Verification:
  - `openclaw doctor` no longer reports legacy session-state or orphan/missing transcript warnings
  - main session store is now 13 entries

## 2026-03-07 12:45 UTC — Security critical cleared and legacy proactive path retired
- Removed risky small-model fallbacks from the live default chain:
  - deleted `groq/llama-3.3-70b-versatile`
  - deleted `cerebras/gpt-oss-120b`
- Current default fallback chain is now:
  - `google/gemini-2.5-flash`
  - `zai/glm-4.7-Flash`
  - `nvidia/moonshotai/kimi-k2-thinking`
- Verification: `openclaw security audit --deep` now reports `0 critical`, leaving only the expected loopback-only `trusted_proxies_missing` warning.
- Retired the old proactive shell path:
  - converted `scripts/proactive-check.sh` and `scripts/proactive-morning.sh` into explicit deprecation stubs
  - updated `scripts/oc-self-heal.sh` to treat `HEARTBEAT_OWNED_PROACTIVE=1` as the healthy state
  - confirmed no user crontab or user timer actually schedules those legacy scripts anymore

## 2026-03-07 12:46 UTC — ZAI cron fragility reduced for failing loops
- Moved the observed failing unattended jobs from `zai/glm-4.7-Flash` to `nvidia/qwen/qwen3.5-397b-a17b`:
  - `Infra Autopilot Loop`
  - `Night Infra Delegation`
  - `Travel Autopilot Loop`
- Verification via manual debug runs:
  - `Infra Autopilot Loop` changed from repeated `Unhandled stop reason: network_error` on ZAI to `status: ok`, summary `INFRA_LOOP_OK`, provider `nvidia`
  - `Travel Autopilot Loop` changed from recent `Unhandled stop reason: network_error` on ZAI to `status: ok`, summary `TRAVEL_LOOP_OK`, provider `nvidia`
- Earlier in the same cleanup pass, `Coding Status Push` was also moved to NVIDIA and verified delivered.

## 2026-03-07 12:53 UTC — Night infra sprint disk triage hardening
- Confirmed the current top autonomous infra task from TODO + latest artifacts/logs is `Monitor disk usage` because `infra-status` still reports `/` at `92%` used.
- Added shared disk-pressure triage in `tools/infra_disk.py` and wired both `tools/infra-status.py` and `tools/infra-autopilot.py` to use it.
- Hardened the disk report to surface safe, non-destructive reclaim signals during root pressure:
  - reviewable reclaim candidates for `/tmp`, `/var/cache/apt`, `/var/log/journal`, and `/home/ubuntu/.cache`
  - top stale `/tmp` entries older than 24h with size + mtime
  - deleted-but-open file detection via `lsof +L1`
- Verification:
  - `python3 -m py_compile tools/infra_disk.py tools/infra-status.py tools/infra-autopilot.py` passed
  - `python3 tools/infra-status.py` generated `departments/infra/artifacts/checks/20260307T125354Z-infra-status.md`
  - `python3 tools/department-commands.py run infra` generated `departments/infra/artifacts/checks/20260307T125352Z-r74-monitor-disk-usage-alert-if-80.md`
  - new artifacts confirmed at mode `600`
- Live disk findings now call out concrete reclaim targets:
  - `/tmp` `989M`, including stale `/tmp/gradle-home` at `913M`
  - `/var/cache/apt` `661M`
  - `/home/ubuntu/.cache` `366M`
  - `/var/log/journal` `130M`
  - no deleted-but-open files detected
- Remaining host risks unchanged outside repo scope:
  - root filesystem `/` still `92%` used
  - unexpected `udp/5353` listener still exposed
  - `ufw` unavailable and no host firewall tool detected from current host checks
  - `1` package update pending

## 2026-03-07 13:03 UTC — Patch layer repaired and housekeeping cron hardened
- Refreshed `tools/patch-openclaw-reply.py` so it no longer assumes the old `reply-prefix-*` bundle layout:
  - added `OPENCLAW_DIST` / `OPENCLAW_PI_AI_DIST` env overrides for safe temp-copy testing
  - retargeted reply patches across the current `reply-*`, `pi-embedded-*`, and `subagent-registry-*` bundles
  - retargeted the Nemotron think-tag patch across all matching `image-*` bundles
- Verification:
  - `python3 -m py_compile tools/patch-openclaw-reply.py` passed
  - temp-dist run under `/tmp/oc-patch-test` applied the missing reply/image patches cleanly and reran idempotently
  - live gateway restart at `13:03 UTC` no longer logged the stale `ctx% targets not found` / `prefix-spacing target not found` warnings
- Hardened unattended infra housekeeping jobs in live `~/.openclaw/cron/jobs.json`:
  - `Daily Session Retention Cleanup` now uses `nvidia/qwen/qwen3.5-397b-a17b`, returns script stdout exactly, and runs with `delivery.mode = "none"`
  - `Daily Config Hygiene` and `Weekly Agent Drift Report` now also pin `nvidia/qwen/qwen3.5-397b-a17b`
- New runtime finding:
  - gateway restart logs show `KillMode=process` leaves child `bash` / `timeout` / `node` / `codex` processes behind during restart, so service child-process cleanup remains an open reliability issue
- Remaining caveat:
  - a manual `Daily Session Retention Cleanup` verification rerun was still active at the end of this pass, so the post-fix cron row itself still needs a completed green finish record

## 2026-03-07 13:24 UTC — Restart cleanup fixed, retention verified, and cron noise reduced
- Installed a managed gateway override at `systemd/openclaw-gateway.override.conf` and synced it to the live user override:
  - switched gateway stop behavior to `KillMode=control-group`
  - capped shutdown with `TimeoutStopSec=15`
- Verification:
  - after the `13:13 UTC` restart, the orphaned `Night Coding Delegation` `bash` / `timeout` / `codex` process tree disappeared
  - stale `runningAtMs` markers for `Night Coding Delegation` and `Daily Session Retention Cleanup` were cleared from `~/.openclaw/cron/jobs.json`
- Fresh cron verification:
  - `Daily Session Retention Cleanup` manual run at `13:14 UTC` finished `status: ok` on NVIDIA with summary `SESSION_RETENTION_OK`
  - `SSH Hardening Reminder` payload was corrected to return message text only (no direct messaging tool call); manual run at `13:23 UTC` finished `status: ok` with delivery success
  - `openclaw cron list` is now fully green across the live managed jobs
- Reduced provider quota noise:
  - updated `tools/check-provider-quota.py` to ignore legacy-only providers from `agents/main/agent/models.json` when they have no active sessions
  - local `--stdout` verification no longer includes the stale OpenRouter error section
- Heartbeat caveat still remains:
  - a manual `openclaw system event --mode now` was accepted by the gateway but did not populate `openclaw system heartbeat last`
  - repeated gateway restarts reset the heartbeat timer; as of `2026-03-07 13:24 UTC`, the recorded heartbeat state is still `null`

## 2026-03-07 13:35 UTC — Night infra sprint disk-pressure hardening
- Reconfirmed the top infra task from `departments/infra/TODO.md` + latest infra logs was `Monitor disk usage` while `/` was still at `92%` and the infra runner kept prioritizing disk pressure.
- Added a guarded stale-temp cleanup helper at `tools/infra_tmp_cleanup.py`:
  - dry-run by default
  - `--apply` requires explicit `--path` values
  - only accepts direct `/tmp` children owned by the current user, older than the age threshold, not protected, and not open in `lsof`
- Wired `tools/infra_disk.py` to advertise the helper during high `/tmp` pressure so future disk artifacts include a review/apply path instead of only raw reclaim hints.
- Executed the helper against validated stale temp directories only:
  - removed `/tmp/gradle-home` (`913M`)
  - removed `/tmp/node-compile-cache` (`28M`)
  - removed `/tmp/jiti` (`14M`)
  - removed `/tmp/goodwy-contacts` (`12M`)
  - removed `/tmp/opencontacts-clean` (`2.0M`)
- Verification:
  - `python3 -m py_compile tools/infra_tmp_cleanup.py tools/infra_disk.py tools/infra-status.py tools/infra-autopilot.py` passed
  - helper dry-run showed the exact five cleanup candidates before deletion
  - helper apply reclaimed `969M`
  - `df -hP /` improved from `92%` to `87%` used (`2.5G` free)
  - fresh status artifact `departments/infra/artifacts/checks/20260307T133430Z-infra-status.md` now shows `/tmp` at `29M` and root usage at `87%`
  - a fresh infra run (`run 77`) shifted top priority from disk back to `Run security audit`, confirming disk is no longer the highest-severity autonomous issue
- Remaining host risks:
  - unexpected external `udp/5353` listener still exposed
  - `ufw` unavailable and no host firewall tool detected
  - `1` package update still pending

## 2026-03-07 13:42 UTC — Proactive delivery moved off heartbeat and verified
- Replaced heartbeat as the outbound proactive path after confirming the installed OpenClaw build blocks direct-chat heartbeat delivery and keeps `heartbeat last` only in process memory.
- Added `tools/proactive-pulse.py`:
  - ranks concrete signals from live cron state, latest infra artifacts, runtime config, and unresolved high-priority action items
  - emits one Telegram-ready suggestion or `NO_REPLY`
  - suppresses duplicate reminders for a per-issue cooldown window via `memory/proactive-pulse-state.json`
- Applied runtime changes through `config-protocol.py`:
  - `agents.defaults.heartbeat.target = "none"`
  - repurposed cron job id `0fd93f83-bf5d-4c7c-9efe-632f5d85ca8a` from `Infra Findings Push` to `Proactive Pulse`
  - schedule: `20 8-22 * * *` in `Australia/Sydney`
  - delivery: Telegram announce to `156480904`
  - payload: run `python3 /home/ubuntu/.openclaw/workspace/tools/proactive-pulse.py` and return stdout exactly
- Aligned drift tooling with the new architecture:
  - updated `scripts/proactive-check.sh` and `scripts/proactive-morning.sh` stubs to mark `PROACTIVE_PULSE_OWNED=1`
  - updated `scripts/oc-self-heal.sh` to treat Proactive Pulse ownership as healthy and heartbeat ownership as stale
- Verification:
  - `python3 -m py_compile tools/proactive-pulse.py` passed
  - local dry-run selected the still-open GitHub secret-scanning blocker after disk pressure had already dropped from `92%` to `87%`
  - manual `openclaw cron run 0fd93f83-bf5d-4c7c-9efe-632f5d85ca8a --expect-final --timeout 180000` completed `status: ok`, provider `nvidia`, model `qwen/qwen3.5-397b-a17b`, and `deliveryStatus: delivered`
  - the delivered summary was: `Coding autonomy is still blocked by GitHub secret scanning on push history...`
  - immediate `python3 tools/proactive-pulse.py --dry-run --debug` returned `NO_REPLY` with `suppressed: true`, proving duplicate suppression is armed
- Follow-up note:
  - the first live Proactive Pulse run consumed `27,037` input tokens because isolated cron jobs still require `agentTurn` in this OpenClaw build; if token burn matters, move the job to a leaner dedicated agent or lower its cadence

## 2026-03-07 15:03 UTC — Night coding sprint shared-core contracts
- Picked the top Ready coding kanban card: `Android shared core: define modular architecture + API contracts`.
- Created repo branch `feature/data-brokerage-android-shared-core-architecture-contracts-2026-03-04` in `opencontacts-map-graph`.
- Hardened the shared-core contract layer:
  - enabled Kotlin explicit API mode for `core-db`, `core-map`, `core-media`, and `core-sync`
  - hid in-memory implementations behind interface-returning factory objects
  - rewired `app` `SharedCoreRegistry` to compose only the public shared-core interfaces
  - expanded `docs/android-shared-core-architecture.md` with dependency and DTO stability rules
  - upgraded the four module contract suites to verify behavior, factory return types, DTO constructor signatures, and source/build boundary rules
- Verification:
  - `bash opencontacts-map-graph/scripts/run-shared-core-contract-tests.sh` passed
  - result detail: `CoreDbContractsTest` 4 passed, `CoreMapContractsTest` 4 passed, `CoreMediaContractsTest` 4 passed, `CoreSyncContractsTest` 4 passed
- Kanban updated: task moved from `Ready` to `Review` with test evidence captured in `departments/coding/kanban/data-brokerage.md`

## 2026-03-07 14:17 UTC — Coding push blocker narrowed to poisoned history
- Investigated the `Coding Day Loop` push failures and confirmed the problem is not current dirty files alone:
  - the active branch `feature/data-brokerage-android-map-ecosystem-shared-core-epic-sprint2-2026-03-06` is `296` commits ahead of `origin/main`, `9` behind, and has no merge base with `origin/main`
  - GitHub push protection is blocking branch history that still contains past secrets, so ordinary retries can never succeed
- Sanitized the tracked workspace tree:
  - removed hardcoded provider keys from `system/agents/main/agent/models.json`, `system/agents/main/agent/auth*.json`, and `tools/test-models.py`
  - removed the hardcoded GitHub token fallback from `scripts/sync-obsidian-bidirectional.sh` and changed push auth to a transient HTTP header instead of storing the PAT in remote URLs
  - redacted secret-bearing archive docs and `memory/SESSION-SUMMARY-2026-03-03.md`
  - redacted the tracked PAT line in `logs/night-coding.log`
  - deleted tracked `system/openclaw.json.bak-2026-03-03` because it was a secret-bearing backup copy
- Added `tools/git-secret-scan.py` and hardened `tools/coding-git-enforce.sh`:
  - autonomous staging now excludes `logs/**`, `memory/SESSION-SUMMARY-*`, `system/*.bak-*`, `**/__pycache__/**`, and `**/*.pyc`
  - staged changes are scanned before commit
  - tracked files are scanned before push
  - branch history reachable from `HEAD` is scanned before push, so future coding loops fail immediately on poisoned history instead of wasting time on doomed `git push` attempts
- Verification:
  - `python3 tools/git-secret-scan.py --repo /home/ubuntu/.openclaw/workspace` → `SECRET_SCAN_OK`
  - `python3 tools/git-secret-scan.py --repo /home/ubuntu/.openclaw/workspace --history-ref HEAD --max-commits 400` → `SECRET_SCAN_FAIL` with grouped historical hits across the old docs/logs/config copies, which matches the GitHub push-protection diagnosis
  - `bash -n tools/coding-git-enforce.sh`, `bash -n scripts/sync-obsidian-bidirectional.sh`, and `python3 -m py_compile tools/git-secret-scan.py tools/test-models.py` all passed
- Remaining blocker:
  - the repo still needs a clean branch snapshot/orphan rescue branch built from the sanitized current tree before autonomous pushes can succeed again
