# 2026-03-13

## Infra Auto-Update Reconciliation
- Investigated why infra did not apply package updates before Jim updated manually.
- Confirmed the root cause was control-plane drift:
  - infra automation only reported `apt list --upgradable`
  - live unattended-upgrades config was already enabled on the host
  - infra TODO still showed `Approve auto-updates` as open
- Added shared auto-update health logic in `tools/infra_update_health.py`.
- Updated `tools/infra-status.py` to report auto-update enablement plus the latest unattended-upgrades run state.
- Updated `tools/infra-autopilot.py` so healthy pending updates stay low-priority, while disabled/drifted or incomplete unattended-upgrades state scores as a real update risk.
- Reconciled `departments/infra/TODO.md` to mark auto-updates approved/enabled on 2026-03-13.
- Verification passed:
  - `python3 -m py_compile tools/infra_update_health.py tools/infra-status.py tools/infra-autopilot.py`
  - `python3 -m unittest tests.test_infra_update_health tests.test_infra_status tests.test_infra_autopilot`
  - `python3 tools/infra-status.py`
  - `python3 tools/department-commands.py run infra`
- Fresh status artifact `departments/infra/artifacts/checks/20260313T024953Z-infra-status.md` now shows:
  - `No pending updates`
  - `INFO: auto-updates enabled`
  - `WARN: unattended-upgrades last started at 2026-03-12 14:35 UTC ... but no completion was logged`
- Result: infra run 208 returned to the higher-risk security audit instead of treating updates as the top issue.

## Infra Auto-Update Recovery Signal Cleanup
- Investigated the remaining unattended-upgrades anomaly from `2026-03-12 14:35 UTC`.
- Verified there was no active `apt`/`dpkg` worker, no pending updates, and Jim's manual `apt upgrade` completed cleanly at `2026-03-13 02:32 UTC`.
- Determined the host was not currently broken; the remaining issue was stale anomaly reporting for an old incomplete unattended-upgrades start.
- Updated `tools/infra_update_health.py` so incomplete unattended-upgrades runs downgrade to informational status when no pending updates remain.
- Verification passed:
  - `python3 -m unittest tests.test_infra_update_health tests.test_infra_status tests.test_infra_autopilot`
  - `python3 tools/infra-status.py`
  - `python3 tools/department-commands.py run infra`
- Fresh status artifact `departments/infra/artifacts/checks/20260313T025530Z-infra-status.md` now reports:
  - `No pending updates`
  - `INFO: auto-updates enabled`
  - `INFO: unattended-upgrades last started ... but no completion was logged; no pending updates remain`
- Result: update health is no longer surfaced as an active infra risk; run 209 stayed focused on the higher-priority SSH/firewall findings.

## Infra Host Hardening Apply
- Applied the managed SSH hardening drop-in live at `/etc/ssh/sshd_config.d/99-openclaw-hardening.conf`.
- Verified the live drop-in with `python3 tools/infra_sshd_hardening.py --validate-live` => `LIVE_VALIDATION_DONE`.
- Confirmed the host uses socket-activated SSH on `ssh.socket`; `ssh.service` is intentionally inactive between connections, so reload-by-service is not the right health signal on this host.
- Verified live firewall state from an unrestricted shell:
  - `ufw` is active
  - default incoming policy is deny
  - SSH/HTTP/HTTPS and `18789/tcp` are allowed
  - `5353/udp` is denied
- Installed the managed fail2ban jail live at `/etc/fail2ban/jail.d/99-openclaw-sshd.local`.
- Verified live fail2ban state from an unrestricted shell:
  - `fail2ban.service` active
  - `sshd` jail active
  - 15 IPs currently banned
- Reconciled `departments/infra/TODO.md` so `Approve UFW installation` is now completed from live host state.
- Current limitation: sandboxed infra status still cannot run privileged `ufw`/`fail2ban` inspection directly, so the normal report may still show blocked `ufw` visibility unless a separate reporting reconciliation is added.

## Messaging Noise Reduction
- Investigated Telegram spam and confirmed the live runtime still had multiple outbound announce jobs:
  - `Coding Status Push`
  - `Finance Status Push`
  - `Provider Quota Check`
  - `SSH Hardening Reminder`
  - `Proactive Pulse`
- Reworked [tools/proactive-pulse.py](/home/ubuntu/.openclaw/workspace/tools/proactive-pulse.py) so it only emits one concise Sydney-time digest in the morning slot and one in the evening slot.
- New pulse behavior:
  - returns `NO_REPLY` outside the morning/evening windows
  - sends at most once per local slot
  - prefixes `IMPORTANT:` only when the digest contains a human decision/TODO
  - keeps quiet background text short (`No action for you. No material issues.`)
- Added regression coverage in [tests/test_proactive_pulse.py](/home/ubuntu/.openclaw/workspace/tests/test_proactive_pulse.py).
- Applied live runtime config via `tools/config-protocol.py`:
  - `cron.jobs["Coding Status Push"].delivery.mode = "none"`
  - `cron.jobs["Finance Status Push"].delivery.mode = "none"`
  - `cron.jobs["Provider Quota Check"].delivery.mode = "none"`
  - `cron.jobs["SSH Hardening Reminder"].delivery.mode = "none"`
  - `cron.jobs["Proactive Pulse"].schedule.expr = "20 8,18 * * *"`
- Verification passed:
  - `python3 -m py_compile tools/proactive-pulse.py tests/test_proactive_pulse.py`
  - `python3 -m unittest tests.test_proactive_pulse`
  - `python3 tools/config-protocol.py status` => no pending plan
  - live `~/.openclaw/cron/jobs.json` now shows the four noisy push jobs on `delivery.mode = "none"` and `Proactive Pulse` scheduled only for `08:20` and `18:20` Australia/Sydney
