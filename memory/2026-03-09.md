# 2026-03-09 - GitHub DNS Resolved & Research Report Feature Pushed

## Major Completion: GitHub DNS Issue Resolved
- ✅ GitHub DNS now functional (`github.com` resolves to `4.237.22.38`)
- ✅ Research report feature (commit `3ecfc39`) successfully pushed to origin
- Branch: `feature/research-reports-first-offer-outline-2026-03-08-workspace`
- Remote URL: `https://github.com/Pandora-jk/opencontacts-map-graph/pull/new/feature/research-reports-first-offer-outline-2026-03-08-workspace`

## Files Pushed
- `departments/coding/kanban/research-reports.md` (updated to Review)
- `research-reports/openclaw-income-tracks-first-offer-outline.md`
- `research-reports/report-outline-template.md`
- `tools/report_outline_validator.py`
- `tests/test_report_outline_validator.py`

## Notes
- Feature was in worktree at `/tmp/research-reports-first-offer-outline`
- Worktree prevented direct `git checkout` from main workspace
- Push completed successfully; PR URL available for review

## 2026-03-09 12:32 UTC — Infra disk-pressure hardening
- Confirmed from `departments/infra/TODO.md` + `logs/infra-activity.log` that the top infra task had shifted from mDNS audit work to `Monitor disk usage`, because fresh infra artifacts escalated root `/` usage to `100%`.
- Hardened `tools/infra_disk.py` so high-pressure disk reports now include review-only hotspots under `/home/ubuntu`, which exposed the main hidden consumers: `~/.gradle` (`2.3G`), `~/.gradle/caches` (`1.9G`), and `~/.npm-global` (`1.8G`).
- Added regression coverage in `tests/test_infra_disk.py`.
- Verification passed:
  - `python3 -m unittest tests.test_infra_disk tests.test_infra_status`
  - `python3 -m py_compile tools/infra_disk.py tools/infra-status.py tools/infra-autopilot.py tools/infra_tmp_cleanup.py`
  - `python3 tools/infra-status.py`
  - `python3 tools/department-commands.py run infra`
- Fresh artifacts:
  - `departments/infra/artifacts/checks/20260309T123252Z-r55-monitor-disk-usage-alert-if-80.md`
  - `departments/infra/artifacts/checks/20260309T123254Z-infra-status.md`

## 2026-03-09 14:49 UTC — Infra disk triage now separates reclaimable caches from protected installs
- Reconfirmed from `departments/infra/TODO.md`, `logs/infra-activity.log`, and fresh runs 60-61 that `Monitor disk usage` remains the top infra task because `/` still reports `100%` usage.
- Hardened the repo-side disk tooling so critical artifacts no longer leave large install roots looking like safe cleanup targets:
  - `tools/infra_disk.py` now emits a `Protected install roots under /home/ubuntu` section for manual-review-only directories like `~/.npm-global`, `~/.android-sdk`, `~/.local/share/pipx/venvs`, and `~/.local/share/claude/versions`
  - `tools/infra_home_cache_cleanup.py` now also allowlists `~/.npm` as an npm download cache for future review/apply cleanup when it grows large enough
- Added regression coverage in `tests/test_infra_disk.py` and `tests/test_infra_home_cache_cleanup.py`.
- Verification passed:
  - `python3 -m unittest tests.test_infra_disk tests.test_infra_home_cache_cleanup tests.test_infra_status`
  - `python3 -m py_compile tools/infra_disk.py tools/infra_home_cache_cleanup.py tools/infra-status.py tools/infra-autopilot.py tools/infra_tmp_cleanup.py`
  - `python3 tools/infra-status.py`
  - `python3 tools/department-commands.py run infra`
- Fresh artifacts:
  - `departments/infra/artifacts/checks/20260309T144901Z-r61-monitor-disk-usage-alert-if-80.md`
  - `departments/infra/artifacts/checks/20260309T144852Z-infra-status.md`

## 2026-03-09 15:40 UTC — Infra disk recovery guidance now sizes the safe cleanup bundle
- Reconfirmed from `departments/infra/TODO.md`, `logs/infra-activity.log`, and fresh runs 63-64 that `Monitor disk usage` remains the top infra task because `/` still reports `100%` usage.
- Hardened the repo-side disk-pressure guidance so it now quantifies what the safe cache cleanup can and cannot fix:
  - `tools/infra_home_cache_cleanup.py` now provides reclaim-bundle sizing helpers for allowlisted home caches
  - `tools/infra_disk.py` now computes the bytes needed to get `/` back under `<=90%` and `<=80%`, recommends the smallest allowlisted home-cache bundle that satisfies the target when possible, and prints the remaining shortfall when it is not
  - fixed a live bug in the new planner by switching from `df -B1P` to `df -B1 -P`; the former was being parsed by GNU `df` as petabyte blocks and produced bogus `1B` reclaim targets
- Fresh live artifacts now show:
  - allowlisted home caches can reclaim enough to get `/` under the critical band (`<=90%`) with `~/.gradle/caches`
  - all allowlisted home caches together reclaim about `2.5G`, still leaving a `995M` shortfall to fully clear the `<=80%` alert target
- Added regression coverage in `tests/test_infra_disk.py` and `tests/test_infra_home_cache_cleanup.py`.
- Verification passed:
  - `python3 -m unittest tests.test_infra_disk tests.test_infra_home_cache_cleanup tests.test_infra_status`
  - `python3 -m py_compile tools/infra_disk.py tools/infra_home_cache_cleanup.py tools/infra-status.py tools/infra-autopilot.py tools/infra_tmp_cleanup.py`
  - `python3 tools/infra-status.py`
  - `python3 tools/department-commands.py run infra`
- Fresh artifacts:
  - `departments/infra/artifacts/checks/20260309T154025Z-r64-monitor-disk-usage-alert-if-80.md`
  - `departments/infra/artifacts/checks/20260309T154031Z-infra-status.md`

## 2026-03-09 16:39 UTC — Night infra sprint routed through department run
- Routed the user-requested infra execution through `python3 tools/department-commands.py run infra` per workspace department-ownership rules.
- Reconfirmed from `departments/infra/TODO.md`, `logs/infra-activity.log`, and run 66 that `Monitor disk usage` is still the top infra reliability task because root `/` remains at `100%` used.
- Verified the latest artifact `departments/infra/artifacts/checks/20260309T163936Z-r66-monitor-disk-usage-alert-if-80.md` still reflects the hardened review-first guidance from the earlier disk-pressure changes:
  - immediate safe relief path remains `~/.gradle/caches` (`1.8G`) to get back under `<=90%`
  - all allowlisted home caches total about `2.5G`, still leaving roughly `997M` to reach `<=80%`
  - protected install roots like `~/.npm-global` and `~/.android-sdk` remain explicitly separated from safe cleanup targets
- No new code delta was produced by run 66; it generated a fresh verification artifact against the existing uncommitted infra hardening already present in the worktree.
